# Laravel Netatmo Weather

WIP project to create a package to get weather data from Netatmo API.

Add config

    php artisan vendor:publish --tag=config

Set .env keys:

    MEMORY_VEHICLE_MODULE=true

